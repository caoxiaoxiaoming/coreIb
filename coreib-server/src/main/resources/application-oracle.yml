coreib:
  datasource:
    enabled: true
    vendor: oracle
    jdbc-url: ${COREIB_DB_URL:jdbc:oracle:thin:@//localhost:1521/FREEPDB1}
    username: ${COREIB_DB_USERNAME:coreib}
    password: ${COREIB_DB_PASSWORD:}
