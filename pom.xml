<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.coreib</groupId>
    <artifactId>coreib</artifactId>
    <version>1.0.1</version>
    <packaging>pom</packaging>
    <name>coreIb</name>
    <description>Business application foundation</description>
    <url>https://github.com/caoxiaoxiaoming/coreIb</url>

    <scm>
        <connection>scm:git:https://github.com/caoxiaoxiaoming/coreIb.git</connection>
        <developerConnection>scm:git:https://github.com/caoxiaoxiaoming/coreIb.git</developerConnection>
        <url>https://github.com/caoxiaoxiaoming/coreIb</url>
        <tag>HEAD</tag>
    </scm>

    <distributionManagement>
        <repository>
            <id>github</id>
            <name>GitHub Packages</name>
            <url>https://maven.pkg.github.com/caoxiaoxiaoming/coreIb</url>
        </repository>
    </distributionManagement>

    <modules>
        <module>coreib-bom</module>
        <module>coreib-kernel</module>
        <module>coreib-data</module>
        <module>coreib-security</module>
        <module>coreib-platform</module>
        <module>coreib-starter-jdbc</module>
        <module>coreib-starter-security</module>
        <module>coreib-starter-web</module>
        <module>coreib-server</module>
    </modules>

    <properties>
        <java.version>17</java.version>
        <maven.compiler.release>${java.version}</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.build.outputTimestamp>2026-09-11T00:00:00Z</project.build.outputTimestamp>
        <spring-boot.version>4.1.0</spring-boot.version>
        <coreib.liquibase.version>5.0.3</coreib.liquibase.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring-boot.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.14.0</version>
                    <configuration>
                        <release>${maven.compiler.release}</release>
                        <parameters>true</parameters>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
